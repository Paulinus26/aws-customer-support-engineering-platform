{
  "commands": [
    "mkdir -p /home/ec2-user/app",
    "cd /home/ec2-user/app",
    "if [ ! -d .git ]; then git clone https://github.com/Paulinus26/aws-customer-support-engineering-platform.git .; else git fetch --all && git reset --hard origin/main; fi",
    "APP_DIR=\",
    "cd \"\\"",
    "npm install",
    "export NODE_TLS_REJECT_UNAUTHORIZED=0",
    "pkill -f node || true",
    "sudo -u ec2-user PM2_HOME=/home/ec2-user/.pm2 pm2 delete all || true",
    "sudo -u ec2-user NODE_TLS_REJECT_UNAUTHORIZED=0 PM2_HOME=/home/ec2-user/.pm2 pm2 start app.js --name supportdesk --update-env || NODE_TLS_REJECT_UNAUTHORIZED=0 nohup node app.js > app.log 2>&1 &"
  ]
}
